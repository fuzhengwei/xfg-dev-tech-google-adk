server:
  port: 8901

logging:
  level:
    root: info
  config: classpath:logback-spring.xml

# Google ADK Configuration
google:
  api:
    key: ${GOOGLE_API_KEY:}
  cloud:
    project: ${GOOGLE_CLOUD_PROJECT:}